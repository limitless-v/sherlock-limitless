"""Optional LLM layer (roadmap section 18).

Ollama is completely optional (OLLAMA_ENABLED=false default). It is used
only for summarization and information extraction — never for face
detection, embedding, verification, or similarity.
"""