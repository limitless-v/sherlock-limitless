"""Platform adapters — scaffold.

Public-page parsers for candidate metadata (display name, username,
image URLs). Read-only access to public pages only.
"""

# Platform-specific parsers (public pages only) — implementation phase
