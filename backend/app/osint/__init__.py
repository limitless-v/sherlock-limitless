"""Internet OSINT layer.

Agent Reach is the discovery layer and is isolated behind an adapter so
third-party types never spread through the application (roadmap section
17). It never performs facial verification.
"""
